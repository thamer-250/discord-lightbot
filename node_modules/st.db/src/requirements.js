const exp = require('express');
const app = exp();

const ApiOperator = (dbName, data) => {
      app.listen();
      app.get(`/${dbName}`, (req, res) => {
        res.send(data)
      })
};

module.exports = {
    ApiOperator,
};